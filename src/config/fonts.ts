import { Rubik, Montserrat_Alternates } from "next/font/google";

export const rubik = Rubik({ subsets: ['latin'], weight: ['400', '700', '900'] });

export const titleFont = Montserrat_Alternates({
    subsets: ["latin"],
    weight: ['500', '700', '900']
});